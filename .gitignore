# dependencies (regenerated with: cd _tools && npm install)
_tools/node_modules/
site/node_modules/

# secrets — never commit real credentials (production values live in Vercel)
.env
.env.local
site/.env
site/.env.local

# macOS
.DS_Store

# local safety backups (kept on disk, not in the repo)
*.bak
*.bak2
*.orig
*.bak-yellow
